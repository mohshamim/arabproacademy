# Preferred on Hostinger (avoids # @ % breaking the URL)
MYSQL_USER="u000000000_arabpro"
MYSQL_PASSWORD="your_mysql_password_here"
MYSQL_HOST="localhost"
MYSQL_PORT="3306"
MYSQL_DATABASE="u000000000_arabpro"

# Optional alternative (only if password has NO special chars, or is properly encoded)
# DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DB_NAME"

# Auth.js — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=
AUTH_URL="https://your-domain.com"

# First SUPER_ADMIN (seed / bootstrap)
ADMIN_EMAIL="admin@arabproacademy.com"
ADMIN_PASSWORD="ChangeMe123!"
ADMIN_NAME="Arab Pro Academy Admin"
